#!/bin/sh

STS=`ps -ef | grep "port-forward" | grep "8080"|wc -l`

if [ $STS -gt 0 ]; then
    echo "already running port-forward 8080:443"
    exit $?
fi

/usr/bin/nohup \
kubectl port-forward svc/argocd-server -n argocd 8080:443 \
>/dev/null 2>&1 &
